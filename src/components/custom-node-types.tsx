import { CustomDeviceNode } from "./custom-device-node";
import { CustomPacketNode } from "./custom-static-packet-node";

export const nodeTypes = {
    device: CustomDeviceNode,
    packet: CustomPacketNode,
};